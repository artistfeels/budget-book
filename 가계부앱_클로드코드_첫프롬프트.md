# 가계부 웹앱 — Claude Code 첫 프롬프트

아래 전체를 그대로 복사해서 Claude Code에 붙여넣으세요. (엑셀 파일은 프로젝트 폴더에 미리 넣어두고 경로만 맞춰주세요.)

---

## 프로젝트 개요

나 혼자 쓰는 **개인용 가계부 웹앱**을 만들어줘. 로그인/멀티유저 필요 없고, 내 로컬에서 실행하는 싱글 유저 앱이야. 뱅크샐러드에서 내보낸 엑셀을 raw 데이터로 불러와서, 월별로 수입/지출을 보고·분석하고·직접 편집까지 할 수 있는 대시보드가 목표야.

## 기술 스택 (특별한 이유 없으면 이대로)

- React 18 + TypeScript + Vite
- Tailwind CSS
- 차트: Recharts
- 엑셀 파싱: SheetJS(xlsx) — 브라우저에서 파일 업로드로 직접 파싱
- 상태: Zustand (또는 Context로 충분하면 그걸로)
- 저장소: 브라우저 IndexedDB (Dexie.js). 서버 없이 동작해야 하고, 새로고침해도 데이터가 남아야 해. JSON export/import 버튼도 만들어줘 (백업용)
- 날짜 처리: date-fns, 한국어 로케일

## 데이터 소스 명세 (중요)

프로젝트 루트의 `data/` 폴더에 뱅크샐러드 엑셀이 있어. 시트가 2개인데 **두 번째 시트 `가계부 내역`만 사용**해줘. (첫 시트 `뱅샐현황`은 무시)

> ⚠️ **중요**: 지금 준 파일은 최종 데이터가 아니라 샘플이야. 앞으로도 계속 **똑같은 형식(뱅크샐러드 내보내기)**으로 새 파일을 주면서 데이터를 갱신할 거야. 그러니까:
> - 파일 내용을 코드에 하드코딩하지 말고, **컬럼 헤더 이름으로 매핑**해서 파싱해줘 (컬럼 순서가 바뀌어도 동작하게)
> - 시트 이름은 `가계부 내역` 우선 탐색하되, 못 찾으면 헤더에 `날짜/타입/금액`이 있는 시트를 자동으로 찾아줘
> - 분류·결제수단 목록은 아래에 적어둔 값을 **초기 시드**로만 쓰고, 새 파일에 없던 값이 나오면 **자동으로 목록에 추가**해줘 (모르는 값 때문에 import가 실패하면 안 됨)
> - 기간이 겹치는 파일을 여러 번 넣게 되니까 **중복 처리(아래 규칙)가 특히 중요**해

`가계부 내역` 시트 구조 — 1행이 헤더, 2행부터 데이터. 총 1,668건, 기간은 2025-07-19 ~ 2026-07-19:

| 컬럼 | 설명 |
|---|---|
| 날짜 | date (YYYY-MM-DD) |
| 시간 | time (HH:MM:SS) |
| 타입 | `수입` / `지출` / `이체` 3종 |
| 대분류 | 예: 식비, 생활, 카페/간식, 주거/통신 … |
| 소분류 | 예: 배달, 편의점, 커피/음료 … (없으면 `미분류`) |
| 내용 | 가맹점/거래처명 (자유 텍스트) |
| 금액 | 정수. **지출/이체 출금은 음수, 수입/이체 입금은 양수** |
| 화폐 | 전부 `KRW` |
| 결제수단 | 카드/계좌/간편결제 명칭 |
| 메모 | 대부분 비어 있음 |

### 데이터 처리 규칙

1. **금액은 내부적으로 절댓값 + 부호 필드로 정규화**해서 다뤄줘. UI에는 수입은 파란색 `+`, 지출은 빨간색 `-`로 표시.
2. **`이체` 타입은 수입/지출 집계에서 반드시 제외**해줘. 내 계좌 간 이동이라 이걸 넣으면 통계가 다 망가져. 다만 별도 "이체 내역" 탭에서는 볼 수 있게 해줘.
3. 카드대금 결제(`이체` > `카드대금`)도 지출로 이중계상하면 안 됨 — 실제 지출은 이미 카드 사용 시점에 잡혀 있음.

### 중복 이체 자동 제거 (페어 매칭)

내계좌이체는 출금 한 줄, 입금 한 줄로 **같은 금액이 부호만 반대로 두 번** 기록돼. 예:

```
2026-07-18 23:34:36  이체  내계좌이체  네이버페이충전  -120,000  NH주거래우대통장
2026-07-18 23:34:36  이체  내계좌이체  충전 내역       +120,000  네이버페이 머니
```

이걸 import 시점에 **자동으로 하나의 "이체 페어"로 묶어서 접어줘.** 로직:

- 대상: `타입 == 이체` 인 행만
- 매칭 조건: `abs(금액)`이 같고, 부호가 반대이고, **결제수단이 서로 다르고**, 시각 차이가 **±3분 이내**
- 매칭되면 두 행을 `transferPairId`로 묶고 `isPairedTransfer: true` 플래그를 달아서 **모든 집계에서 한 번만(또는 아예 0으로) 반영**되게 처리
- 한 금액에 후보가 여러 개면 **시간이 가장 가까운 것끼리 그리디로 1:1 매칭**하고, 이미 매칭된 행은 재사용 금지
- 짝을 못 찾은 이체(출금만 있고 입금이 없는 경우 등)는 `unmatchedTransfer`로 남기고, import 결과 화면과 이체 탭에서 **경고 뱃지로 따로 보여줘**. 조용히 버리면 안 됨
- 원본 행은 삭제하지 말고 flag만 달아. 사용자가 "이체 페어 펼치기" 토글로 원본을 볼 수 있어야 해
- 이 매칭 로직은 **유닛 테스트 필수** (정상 페어 / 3분 초과 / 금액만 같고 같은 계좌 / 후보 다중 / 짝 없음 케이스)

### 저축성 vs 소비성 구분

돈이 나가는 걸 전부 "지출"로 묶으면 안 돼. **자산으로 남는 돈(저축성)과 사라지는 돈(소비성)을 구분**해서 집계해줘. 각 거래에 `flowType` 필드를 붙여:

| flowType | 정의 | 판정 규칙 |
|---|---|---|
| `income` | 실제 유입 | `타입 == 수입` |
| `saving` | 자산으로 남는 유출 | 아래 규칙 참조 |
| `spending` | 소비되어 사라지는 유출 | 위 둘에 해당 안 되는 `타입 == 지출` |
| `neutral` | 집계 제외 | 페어 매칭된 내계좌이체, 카드대금 |

**`saving` 판정 규칙** (우선순위 순, 위에서부터 매칭):
1. 결제수단이 `주택청약종합저축`, `NH청년도약계좌`, `월세 보증금` 인 유출
2. `타입 == 이체` 이고 대분류가 `투자` 인 건
3. `타입 == 지출` 이고 대분류 `금융` + 소분류 `증권/투자` 인 건
4. 그 외 사용자가 직접 "저축으로 분류" 토글한 건 (수동 오버라이드)

추가 요구사항:
- **수동 오버라이드 기능** 필수. 어떤 거래든 우클릭/메뉴에서 소비성 ↔ 저축성을 바꿀 수 있고, 그 설정은 IndexedDB에 거래 ID 기준으로 저장돼서 다음 import 때도 유지돼야 해
- 오버라이드 규칙을 **"이 가맹점/결제수단은 항상 저축으로"** 형태의 룰로도 저장할 수 있게 (룰 관리 화면 하나)
- 저축성/소비성 규칙은 코드에 흩어놓지 말고 **`src/lib/classification.ts` 한 파일에 모아서** 나중에 내가 쉽게 고칠 수 있게 해줘
- 이 구분에 따라 **KPI를 재정의**해줘:
  - 총수입 = `income` 합계
  - 소비지출 = `spending` 합계
  - 저축·투자 = `saving` 합계
  - 저축률 = (`saving` + (`income` − `spending` − `saving`)) / `income` … 즉 **실질 저축률 = (수입 − 소비지출) / 수입**
  - 순현금흐름 = `income` − `spending` − `saving`
- 대시보드 월별 추이 차트에서 유출 막대를 **소비성/저축성 스택 막대**로 나눠서 보여줘 (색: 소비=rose, 저축=emerald)
- 월별 상세와 카테고리 차트는 **기본적으로 `spending`만** 집계하고, "저축 포함" 토글을 두자

### 실제 존재하는 분류 체계 (이 값들을 드롭다운 기본값으로 하드코딩)

**수입 대분류**: 급여, 상여금, 금융수입, 기타수입, 용돈, 중고거래, 미분류

**이체 대분류**: 내계좌이체, 카드대금, 투자, 현금, 이체, 미분류

**지출 대분류 → 소분류**
- 식비: 한식, 일식, 중식, 양식, 아시아음식, 고기, 치킨, 패스트푸드, 배달, 식재료, 미분류
- 카페/간식: 커피/음료, 베이커리, 디저트/떡, 도넛/핫도그, 아이스크림/빙수, 기타간식, 미분류
- 생활: 편의점, 마트, 생필품, 가구/가전, 생활서비스, 미분류
- 주거/통신: 월세, 관리비, 가스비, 인터넷, 휴대폰
- 교통: 대중교통, 택시, 철도, 시외버스
- 자동차: 주유, 주차, 통행료, 정비/수리
- 온라인쇼핑: 인터넷쇼핑, 결제/충전, 서비스구독, 앱스토어, 미분류
- 패션/쇼핑: 패션, 백화점, 아울렛/몰
- 뷰티/미용: 화장품, 미용용품, 헤어샵, 피부과
- 의료/건강: 약국, 내과/가정의학, 이비인후과, 종합병원, 건강용품, 보조식품, 운동
- 문화/여가: 영화, 도서, 게임, 음악, 취미/체험, 테마파크
- 술/유흥: 맥주/호프, 이자카야, 바(BAR), 와인
- 교육/학습: 학원/강의, 미분류
- 경조/선물: 축의금, 부의금, 선물, 미분류
- 여행/숙박: 숙박비
- 금융: 은행, 카드, 증권/투자, 세금/과태료
- 기타 / 미분류: 미분류

**결제수단** (드롭다운, 사용자가 추가 가능하게):
삼성카드 taptap O, iMND Light 신한카드, 일상의 기쁨(신용) 웰트리 복지비카드, 플리(체크), 네이버페이 간편결제, 네이버페이 간편결제(머니), 네이버페이 간편결제(포인트), 네이버페이 머니, 카카오페이 간편결제, 카카오페이 머니, 토스 간편결제, 삼성월렛머니 우리 통장, NH주거래우대통장, KB나라사랑우대통장, MY 입출금통장, 토스뱅크 통장, OK파킹플렉스통장, NH청년도약계좌, 주택청약종합저축, 월세 보증금, Deep Oil

## 화면 구성

### 1. 메인 대시보드 (`/`)

- 상단: 기간 선택 (전체 / 최근 6개월 / 최근 12개월 / 커스텀)
- **KPI 카드 5개**: 총수입(`income`), 소비지출(`spending`), 저축·투자(`saving`), 실질 저축률(%), 순현금흐름. 각 카드에 전월 대비 증감률 표시
- **월별 추이 차트**: ComposedChart — 수입은 막대, 유출은 소비성/저축성 **스택 막대**, 순현금흐름은 라인. 막대 클릭 시 해당 월 상세 페이지로 이동
- **지출 카테고리 구성**: 도넛 차트(대분류 기준). 조각 클릭 → 소분류로 드릴다운. 오른쪽에 금액·비중 리스트
- **카테고리별 월간 히트맵**: 행=대분류, 열=월, 색 농도=지출액. 어느 달에 어디서 많이 썼는지 한눈에
- **Top 10 가맹점**: `내용` 기준 합계 상위 10개 가로 막대
- **결제수단별 지출 비중**: 작은 파이 차트
- 모든 차트는 hover 툴팁에 금액을 `1,234,567원` 한국식 콤마 포맷으로 표시

### 2. 월별 상세 (`/month/:yyyy-mm`)

- 상단에 월 선택 드롭다운 + 좌우 화살표로 이전/다음 달 이동
- **캘린더 뷰 (핵심)**: 해당 월 월력. 각 날짜 셀에 그 날의 수입 합계(파란색 위쪽), 지출 합계(빨간색 아래쪽)를 작게 표시. 지출 강도에 따라 셀 배경 옅은 그라데이션. 날짜 클릭 시 우측 패널(또는 모달)에 그 날의 거래 리스트가 뜨고, 거기서 바로 추가/수정/삭제 가능
  - **주간 지출 밴드**: 각 주(월~일 7칸)의 **뒤쪽에 가로로 하나로 이어진 옅은 배경 띠**를 깔고, 그 위(또는 띠 오른쪽 끝)에 그 주의 총지출을 표시해줘. 7개 날짜 셀이 이 띠 위에 얹혀 있는 느낌이어야 하고, 띠는 날짜 숫자를 가리지 않게 아주 연한 색(opacity 10~15%)으로. 주간 지출액이 클수록 띠 색이 진해지게
  - 띠에는 `1주차 · 지출 412,000원` 형태로 표시하고, 옆에 그 달 주간 평균 대비 증감(`▲ 12%`)도 작게. 띠를 클릭하면 그 주 거래만 필터링
  - 월 경계 주(전달/다음달 날짜가 섞인 주)는 **해당 월에 속한 날짜만 합산**하고 띠에 "부분 주" 표시
- **지출 속도(Spending Pace) 라인 차트**: 이번 달 지출 페이스가 지난달보다 빠른지 느린지 보는 차트
  - X축 = 1일~31일, Y축 = **누적 지출액**
  - 선 3개: ① 이번 달 누적(굵은 실선, rose) ② 지난달 같은 기간 누적(점선, gray) ③ 최근 3개월 평균 누적(옅은 실선, 참고선)
  - 이번 달 선은 **오늘 날짜까지만 그리고**, 그 이후는 현재 속도 기준 **월말 예상치를 옅은 점선으로 연장**해줘
  - 차트 위에 한 줄 요약: `지난달 같은 날 대비 +18% 빠름 · 이 속도면 월말 예상 2,340,000원`
  - 지난달보다 빠르면 rose, 느리면 emerald로 요약 문구 색 구분
  - `spending`(소비성)만 집계. 저축성은 제외
- **그 달의 요약**: 수입/지출/순저축 + 전월 대비
- **카테고리별 지출 분포**: 도넛 + 대분류별 가로 막대 (금액, 건수, 평균 단가, 전월 대비 증감)
- **인포그래픽 블록**: 예를 들어 "이번 달 가장 많이 쓴 날", "배달 몇 번 / 총 얼마", "커피 지출", "하루 평균 지출", "가장 자주 간 가맹점", "무지출 데이라 며칠" 같은 카드형 위젯. 아이콘 + 큰 숫자 + 한 줄 설명 형태로

### 3. 거래 입력/관리 (`/entries`)

여기가 제일 중요해. **세로로 쭉 나열된 하나의 테이블이 아니라, 탭으로 3개 섹션이 나뉘어야 해:**

- **수입 섹션** — 컬럼: 날짜 / 입금수단 / 대분류 / 내용 / 금액
- **저축·투자 섹션** — 컬럼: 날짜 / 계좌 / 구분(청약·적금·투자 등) / 내용 / 금액
- **지출 섹션** — 컬럼: **날짜 / 결제수단 / 대분류 / 소분류 / 지출내용 / 금액** (이 6개 반드시 다 보이게)

각 섹션 공통 요구사항:
- 상단에 **월 필터**가 있어서 그 달 데이터만 보이게. raw 데이터가 딱 월 단위로 끊겨 있지 않으니(2025-07-19 시작, 2026-07-19 종료), 월 필터는 데이터에 존재하는 월만 자동으로 뽑아서 리스트업해줘. 부분 월(2025년 7월, 2026년 7월)은 "일부 기간" 뱃지로 표시
- **인라인 편집**: 셀 클릭하면 바로 수정. 날짜는 date picker, 대분류/소분류/결제수단은 select(대분류 선택 시 소분류 옵션 자동 연동), 금액은 숫자 입력에 자동 콤마
- **행 추가 버튼**: 테이블 맨 아래 "+ 추가" 누르면 빈 행이 생기고 바로 입력. 날짜는 현재 선택된 월의 오늘(또는 1일)로 기본값
- **행 삭제**, 다중 선택 후 일괄 삭제
- 정렬(날짜·금액), 검색(내용/메모 텍스트), 대분류·결제수단 필터
- 섹션 하단에 **필터 적용된 합계** 고정 표시
- 신규 카테고리/결제수단을 입력창에서 바로 추가할 수 있게 (select에 "직접 입력" 옵션)

### 4. 데이터 불러오기 (`/import`)

- 엑셀 파일 드래그&드롭 업로드 → `가계부 내역` 시트 자동 인식 → 파싱 결과 미리보기 테이블
- **불러올 월을 체크박스로 선택**할 수 있게. 파일 전체를 다 넣는 게 아니라 내가 원하는 달만 골라서 넣는 게 핵심
- 이미 저장된 데이터와 **중복 검사** (날짜+시간+금액+내용 조합이 같으면 중복으로 간주) → 중복 건은 스킵/덮어쓰기 선택
- 불러오기 결과 요약 (신규 N건, 중복 M건, 스킵)
- 초기 실행 시 `data/` 폴더의 파일을 자동으로 읽어오는 옵션도 있으면 좋음

## 디자인

- 깔끔하고 밝은 톤. 카드 기반 레이아웃, 넉넉한 여백, 둥근 모서리, 은은한 그림자
- 수입=blue 계열, 지출=red/rose 계열, 저축=emerald 계열로 색 규칙 통일
- 금액은 전부 한국 원화 포맷(`₩1,234,567` 또는 `1,234,567원`), 큰 숫자는 대시보드에서 `123.4만원` 축약 옵션
- 폰트는 Pretendard (CDN)
- 반응형은 데스크탑 우선, 태블릿까지만 대응하면 충분
- 다크모드는 나중에 — 지금은 넣지 마

## 진행 방식

1. 먼저 `data/` 폴더의 엑셀을 직접 읽어서 실제 값 분포를 확인하고, 파싱 로직이 맞는지 검증해줘
2. 프로젝트 구조 잡고 → 데이터 레이어(타입 정의, 파서, IndexedDB 스키마, 집계 함수) → 대시보드 → 월별 상세 → 입력 화면 → import 순서로
3. 각 단계 끝날 때마다 뭘 만들었는지 짧게 알려주고 다음으로 넘어가줘
4. 집계 로직(이체 제외, 카드대금 이중계상 방지 등)은 **유닛 테스트**를 꼭 붙여줘. 여기 틀리면 숫자가 다 틀림
5. 완성 후 `npm run dev`로 바로 돌릴 수 있게 README에 실행법 정리

먼저 **전체 파일 구조와 데이터 모델 설계안**을 제안해주고, 내가 OK 하면 구현 시작해줘.
